import { CalendarWithMaskDirective } from './calendar-with-mask.directive';

describe('CalendarWithMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarWithMaskDirective();
    expect(directive).toBeTruthy();
  });
});
